<div class="card-grid">
    {{#each cards}} <!-- Assuming 'cards' is passed as context from your route -->
    <div class="card">
        <img src="{{this.image}}" alt="{{this.title}}">
        <h3> "Hey </h3>
        <p>"Chill"</p>
    </div>
    {{/each}}
</div>